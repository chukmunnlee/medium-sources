# To make Grafana persistent (Using Statefulset)
#
grafana:
    persistence:
      enabled: true
      type: sts
      storageClassName: standard
      accessModes:
      - ReadWriteOnce
      size: 2Gi
